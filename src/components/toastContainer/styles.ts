import styled from 'styled-components';

export const Container = styled.div`
  /* pra ficar solto na tela, por cima de tudo */
  position: absolute;
  right: 0;
  top: 0;
  padding: 30px;
  overflow: hidden;
`;


